# The only secret. Long random string, decides the word of the day and signs game ids.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
GAME_SECRET=change-me
